// Aca defino la funcion principal del stick, reservo el bloque de memoria con malloc y pongo a esuchar conexiones del cpu y kernel memory entiendo
#include "stick_utils.h"

void iniciar_operacion_stick(char* config_path, int tamanio) {
    t_memory_stick_info stick;
    stick.tamanio_memoria = tamanio;

    // aca deberia ir lo del logger propio
}
